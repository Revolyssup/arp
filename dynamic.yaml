routes:
  - name: route1
    listener: http
    match:
      path: /
    upstream:
      name: backend1
      nodes:
        - url: http://httpbin.org/headers
        - url: http://httpbin.org/ip
      