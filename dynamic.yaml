routes:
  - name: route1
    listener: http
    matches:
      - path: /
    upstream:
      name: backend1
      nodes:
        - url: http://httpbin.org/headers
        - url: http://httpbin.org/ip
      